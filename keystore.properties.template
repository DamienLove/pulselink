# Copy this file to `keystore.properties` and fill in the values to enable local release signing.
# Paths may be absolute or relative to the project root.
storeFile=pulselink-upload-2025.jks
storePassword=REPLACE_ME
keyAlias=pulselink_upload
keyPassword=REPLACE_ME
